/**
 * * A função net.createServer() cria um servidor TCP.
 * * Ela recebe uma função de callback que é chamada
 * * sempre que uma nova conexão é estabelecida.
 */

/**
 * * O parâmetro socket é um objeto que representa a
 * * conexão com o cliente. É através desse socket que o
 * * servidor irá se comunicar com o cliente.
 */

/**
 * * O evento 'data' é acionado quando o servidor recebe
 * * dados do cliente. A função de callback é chamada com os dados (data).
 */

/**
 * * O método server.listen(serverPort) faz com que o
 * * servidor comece a ouvir na porta definida (7896).
 */
